# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Spendrift is an iOS/iPadOS finance app. The repository is newly initialized and does not yet contain source code, an Xcode project, or build tooling.

## Current State

There are no build, lint, or test commands yet. When the Xcode project (or Swift Package) is added, update this file with:

- How to build the app (e.g., `xcodebuild` invocations or the scheme/destination to use)
- How to run the test suite and a single test
- Any linting/formatting tooling adopted (e.g., SwiftLint, swift-format)
- The high-level architecture once it takes shape
